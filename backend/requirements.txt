# Core runtime. Kept deliberately lean so the image fits a small instance:
# MLflow is optional (see requirements-mlflow.txt) because the code imports it
# lazily and only when MLFLOW_TRACKING_URI is configured.

# ---- API -------------------------------------------------------------------
fastapi==0.109.2
uvicorn[standard]==0.27.1
pydantic==2.6.1
pydantic-settings==2.1.0
python-multipart==0.0.9
email-validator==2.3.0

# ---- persistence -----------------------------------------------------------
SQLAlchemy==2.0.25
alembic==1.13.1
psycopg2-binary==2.9.9
redis==5.0.1

# ---- security --------------------------------------------------------------
python-jose[cryptography]==3.3.0
bcrypt==4.1.2

# ---- streaming -------------------------------------------------------------
kafka-python==2.3.0

# ---- data & ML -------------------------------------------------------------
numpy==1.26.2
pandas==2.2.0
scikit-learn==1.3.2
xgboost==3.2.0
shap==0.51.0
joblib==1.4.2
networkx==3.6.1

# ---- http client (LLM providers) -------------------------------------------
httpx==0.26.0
